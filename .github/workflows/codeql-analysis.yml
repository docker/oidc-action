name: codeql

on:
  push:
    branches:
      - 'master'
      - 'releases/v*'
    paths:
      - 'codeql-analysis.yml'
      - 'dist/**'
      - 'src/**'
  pull_request:
    paths:
      - 'codeql-analysis.yml'
      - 'dist/**'
      - 'src/**'

permissions:
  contents: read

jobs:
  analyze:
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # 6.0.2
        with:
          persist-credentials: false
      - name: Initialize CodeQL
        uses: github/codeql-action/init@e46ed2cbd01164d986452f91f178727624ae40d7 # 4.35.3
        with:
          languages: javascript-typescript
          config: |
            paths:
              - src
      - name: Autobuild
        uses: github/codeql-action/autobuild@e46ed2cbd01164d986452f91f178727624ae40d7 # 4.35.3
      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@e46ed2cbd01164d986452f91f178727624ae40d7 # 4.35.3
        with:
          category: '/language:javascript-typescript'
